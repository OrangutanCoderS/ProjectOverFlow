[package]
name = "filemon"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1"
thiserror = "1"
walkdir = "2"
tracing = "0.1"
tracing-subscriber = "0.3"
chrono = "0.4"
overflow-core = { path = "../core" }
dirs = "5"

[dev-dependencies]
criterion = "0.5"
tempfile = "3"

[features]
default = []
bench = []