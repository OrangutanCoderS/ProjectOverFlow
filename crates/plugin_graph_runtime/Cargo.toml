[package]
name = "plugin_graph_runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hashbrown = "0.15"
thiserror = "1.0"

# Execution node + plugin chain + context graph
autonomous_runtime = { path = "../autonomous_runtime" }
context_graph = { path = "../context_graph" }
execution_node = { path = "../execution_node" }
plugin_response_chain = { path = "../plugin_response_chain" }

[dev-dependencies]
criterion = "0.5"
serde_yaml = "0.9"

