[package]
name = "action_inject_env"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
thiserror = "1.0"
log = "0.4"
anyhow = "1.0"
cfg-if = "1.0"
nix = "0.28.0"

[dev-dependencies]
criterion = "0.5"