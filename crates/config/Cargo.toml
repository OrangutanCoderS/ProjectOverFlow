[package]
name = "config"
version = "0.1.0"
edition = "2021"

[lib]
name = "config"
path = "src/lib.rs"

[dependencies]
thiserror = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
regex = "1"
once_cell = "1"

[dev-dependencies]
criterion = "0.5"
tempfile = "3"