[package]
name = "memory_replay_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "memory_replay_engine"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
chrono = { version = "0.4", features = ["serde"] }
rand = "0.8"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "replay_bench"
harness = false
