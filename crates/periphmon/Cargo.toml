[package]
name = "periphmon"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
once_cell = "1"
regex = "1"
thiserror = "1"
overflow-utils = { path = "../utils" }

[dev-dependencies]
pretty_assertions = "1"
criterion = "0.5"